{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '=== PORT='$PORT && alembic upgrade head && echo '=== importing app' && python -c 'from app.main import app; print(\"=== import OK\")' && echo '=== starting uvicorn' && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --log-level debug",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
